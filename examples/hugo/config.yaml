# Merge these settings into your Hugo configuration.
# The custom output format publishes /search-index.json from the home page.
outputFormats:
  SearchIndex:
    mediaType: application/json
    baseName: search-index
    isPlainText: true
    notAlternative: true

outputs:
  home:
    - html
    - rss
    - SearchIndex
